import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Autobot_finale',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
